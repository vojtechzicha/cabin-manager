/**
 * Domain layer — pure TypeScript. No Payload, Next, Mongo, or React imports.
 * Houses the settlement engine (finance), optimal-date algorithm (scheduling),
 * and lifecycle state-machine guards. Enforced by eslint-plugin-boundaries.
 */
export {};
